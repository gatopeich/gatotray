# Architecture Diagram: Temperature Sensor Selection

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User Interaction                            │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Right-click tray icon
                                  │ → Select "Preferences"
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        GTK Preferences Dialog                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ Temperature sensor: [Dropdown ▼]                              │  │
│  │                                                               │  │
│  │ Options shown:                                                │  │
│  │  • Auto (first available)                                     │  │
│  │  • coretemp (hwmon0 temp1)    ← CPU sensor                   │  │
│  │  • nvme (hwmon1 temp1)        ← SSD sensor                   │  │
│  │  • thermal_zone0              ← Generic thermal zone         │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  User selects → on_temp_sensor_changed() callback                  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Updates preferences
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         settings.c                                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ pref_temp_sensor = selected_path                              │  │
│  │ pref_temp_sensor_path = pref_temp_sensor                      │  │
│  │                                                               │  │
│  │ On Save → pref_save() → writes to ~/.config/gatotrayrc       │  │
│  │                                                               │  │
│  │ On Load → pref_init() → reads from ~/.config/gatotrayrc      │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Provides sensor list
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         cpu_usage.c                                 │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ discover_temp_sensors()                                       │  │
│  │  ├── Scans /sys/class/hwmon/hwmon*/temp*_input               │  │
│  │  ├── Scans /sys/class/thermal/thermal_zone*/temp             │  │
│  │  ├── Scans /proc/acpi/thermal_zone/*/temperature             │  │
│  │  ├── Reads /sys/class/hwmon/hwmon*/name for labels           │  │
│  │  └── Returns: char** paths, char** labels                    │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ cpu_temperature()                                             │  │
│  │  ├── Checks if pref_temp_sensor_path is set                  │  │
│  │  ├── If set: Opens the selected sensor                       │  │
│  │  ├── If not set: Falls back to automatic detection           │  │
│  │  ├── Reads temperature value                                 │  │
│  │  └── Returns: int temperature_celsius                        │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Temperature reading
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         gatotray.c                                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ update_history()                                              │  │
│  │  └── history[0].temp = cpu_temperature()                     │  │
│  │                                                               │  │
│  │ redraw()                                                      │  │
│  │  └── Displays temperature in thermometer graph               │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Visual output
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         System Tray                                 │
│  ┌─────────────────────┐                                           │
│  │   🌡️  [===56°C===]   │  ← Thermometer showing selected sensor  │
│  │   📊  CPU graph      │                                          │
│  │   💾  Memory bar     │                                          │
│  └─────────────────────┘                                           │
└─────────────────────────────────────────────────────────────────────┘


Data Flow:
══════════

1. Initialization:
   discover_temp_sensors() → Finds available sensors
                          ↓
   pref_init() → Loads saved preference from config file
                          ↓
   cpu_temperature() → Opens selected or auto-detected sensor

2. User Configuration:
   User selects sensor in UI → on_temp_sensor_changed()
                             ↓
   Updates pref_temp_sensor_path
                             ↓
   pref_save() → Saves to ~/.config/gatotrayrc
                             ↓
   cpu_temperature() → Reopens new sensor on next call

3. Temperature Monitoring Loop:
   Timer triggers update_history()
                          ↓
   cpu_temperature() → Reads from selected sensor file
                          ↓
   redraw() → Updates thermometer display
                          ↓
   User sees current temperature


Configuration File Structure:
═══════════════════════════════

~/.config/gatotrayrc:
┌───────────────────────────────────────────────┐
│ [Colors]                                      │
│ Free memory (top)=#008080                     │
│ ...                                           │
│                                               │
│ [Options]                                     │
│ Transparent background=true                   │
│ Show Thermometer=true                         │
│ Basic refresh interval (ms)=1000              │
│ ...                                           │
│ Temperature sensor=/sys/class/hwmon/hwmon0/temp1_input  ← NEW
└───────────────────────────────────────────────┘


Error Handling:
═══════════════

Selected sensor file doesn't exist:
  cpu_temperature() logs warning → Falls back to auto-detection

No sensors found:
  cpu_temperature() → Returns 0 (thermometer not shown)

Sensor becomes unavailable:
  cpu_temperature() → Marks as unavailable, stops trying

Preference file doesn't exist:
  pref_init() → Creates with defaults


Backwards Compatibility:
═══════════════════════

Old config (no sensor preference):
  → cpu_temperature() uses original auto-detection
  → Works exactly as before

New config with sensor preference:
  → cpu_temperature() uses selected sensor
  → Falls back to auto if sensor unavailable
